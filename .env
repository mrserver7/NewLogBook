# MongoDB connection string for the new database
MONGODB_URI=mongodb+srv://mrserver7:<Moh@77987>@cluster0.vmivrh.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Secret used to sign sessions.  Keep this value safe.
SESSION_SECRET=supersecretkey

# Auth0 configuration.  Replace these values with your Auth0 tenant
# information.  See README or deployment guide for details.
# Auth0 configuration variables.  These are intentionally left blank so
# that secrets are not committed to version control.  Set these values
# via environment variables in your deployment platform (e.g. Render.com).
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=

# Optionally set the base URL for your application.  This value is used
# by express-openid-connect to construct redirect URLs.  When deploying
# to Render, set this to your Render service URL (e.g. https://example.onrender.com).
BASE_URL=http://localhost:5000

# DATABASE_URL is no longer used now that the application uses MongoDB.
# You may remove this variable entirely if desired.
# DATABASE_URL=postgresql://postgres:mysecretpassword@localhost:5432/postgres

